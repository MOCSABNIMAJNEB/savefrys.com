comments:
  allowedFields: ["name", "email", "message"]
  branch: "staticman"
  commitMessage: "comment! ... {options.slug}"
  filename: "entries"
  format: "json"
  name: "savefrys.com"
  path: "data/comments"
  moderation: false
  requiredFields: ["name", "email", "message"]

