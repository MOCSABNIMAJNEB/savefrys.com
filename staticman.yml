comments:
  allowedFields: ["name", "email", "message"]
  branch: "staticman"
  commitMessage: "comment!"
  filename: "entries-{@timestamp}" 
  format: "json"
  name: "savefrys.com"
  path: "data/comments"
  moderation: false
  requiredFields: ["name", "email", "message"]

